import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['encrypted-tbn0.gstatic.com', 'cloudinary.com', 'res.cloudinary.com'],
  },
};

export default nextConfig;
